sonar.organization=overnightworks
sonar.projectKey=overnightworks_claude-revive

# This project runs on SonarCloud's Automatic Analysis, which reads its
# configuration from the service and ignores this file. The key above is still
# the one owner of the project name: the "SonarCloud open findings" step
# (.github/workflows/ci.yml) reads it from here, so the gate never carries a
# literal that could drift from the project it asks about.
